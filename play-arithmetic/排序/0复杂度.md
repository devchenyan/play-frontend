let nums = [3,2,56,32,47,8,2,1,57,12,25,4]

function swap(array, x, y) {
  const tmpY = array[y]
  array[y] = array[x]
  array[x] = tmpY
}

/**
 *
*/

let sortArray = function quickSort(nums) {
return nums
};
